# Argon One Fan Control Config
# Lines with "tempC:duty" set thresholds and fan duty cycle (%).
# Evaluated ascending; highest matching rule applies.
55:25
65:50
75:100

# Optional settings:
poll_interval=10     # seconds between temperature checks
fan_pin=18           # BCM pin for the fan transistor (Argon One typically GPIO18)
pwm_freq=50          # PWM frequency in Hz
# stop_below=50      # (optional) force fan off when CPU temp < this value
